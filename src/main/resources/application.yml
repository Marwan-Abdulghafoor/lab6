spring:
  application:
    name: ADS
  datasource:
    url: jdbc:mysql://localhost:3306/ads_db
    username: root
    password: golangMarwan
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
